server:
  port: ${PORT:9080}

spring:
  application:
    name: ${SERVICE_NAME:bulletin-board}
  jpa:
    show-sql: true

logging:
  level:
    org.springframework: ${LOGGING_LEVEL_SPRING:INFO}

super:
  duper:
    test:
      phrase: "Hello, bulletin board!"
